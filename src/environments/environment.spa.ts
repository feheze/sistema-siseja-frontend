import { Environment } from './environment.interface';

export const environment: Environment = {
  production: true,
  apiUrl: 'https://api.siseja.com/api',
  appName: 'SISEJA-SPA',
  version: '1.0.0',
  ssr: false
};